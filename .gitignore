# MemPalace per-project files (machine-local config, regenerated by `mempalace init .`)
mempalace.yaml
entities.json

# MemPalace local palace (vector DB — machine-local, rebuilt by mining)
.mempalace/

# graphify derived outputs (auto-rebuilt by the post-commit hook)
graphify-out/

# Claude Code local (per-user) files
.claude/settings.local.json
.claude/CLAUDE.local.md
CLAUDE.local.md

# claude-mem context dump (regenerated each session with a fresh timestamp)
AGENTS.md

# Env & secrets
.env
.env.*
!.env.example

# Dependencies & build artifacts
node_modules/
dist/
build/
__pycache__/
*.pyc
.venv/
venv/

# Vendored cybersecurity-skills library (pinned external dependency, rebuilt
# from the SHA recorded in skills/PINNED.md — never committed, 49MB payload)
vendor/

# OS / editor
.DS_Store
*.swp

# Jac local data (graph DB, caches) — regenerated
.jac/

# Runtime output: logs, audit stream, run artifacts — regenerated
var/
runs/
